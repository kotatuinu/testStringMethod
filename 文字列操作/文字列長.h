#pragma once
#include "constdefine.h"
#include <string>

class •¶š—ñ’·
{
private:
	RESULT ‹Œ(const char* word, size_t& len);
	RESULT V(const std::string& word, size_t& len);

public:
	void exec();
};

